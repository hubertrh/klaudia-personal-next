import about from './homepage/about-schema';
import category from './portfolio/category-schema';
import contact from './homepage/contact-schema';
import entry from './portfolio/entry-schema';
import hero from './homepage/hero-schema';

const schemas = [hero, about, contact, category, entry];

export default schemas;
